# hexa.toml — hexa-chip standalone package manifest
#
# Spec: shared/hexa-lang/hexa-toml-spec.json (version 1.0.0)
# Distribution target: hx install hexa-chip
# Provenance: extracted from canon/domains/compute/ on 2026-05-06
#             (n6-arch SHA c0f1f570; 28-verb 7-group chip substrate)

[package]
name = "hexa-chip"
version = "1.0.0"
entry = "cli/hexa-chip.hexa"
description = "Chip substrate — 29-verb semiconductor stack (architecture / design / EDA / process / packaging / NPU / PIM / 3D / photonic / RTL-gen / yield / consciousness-chip)."
license = "MIT"
authors = ["박민우 <nerve011235@gmail.com>"]
repository = "https://github.com/dancinlab/hexa-chip"
homepage = "https://github.com/dancinlab/hexa-chip"
readme = "README.md"
keywords = [
    "semiconductor", "chip-architecture", "rtl", "eda", "npu", "pim",
    "photonic", "packaging", "hbm", "advanced-packaging", "yield",
    "consciousness-chip", "n6-invariant", "korea-fab"
]
categories = ["hardware", "compute", "semiconductor", "eda"]

[[bin]]
name = "hexa-chip"
path = "cli/hexa-chip.hexa"

[scripts]
install = "install.hexa"

# ── 6-group / 29-verb manifest ─────────────────────────────
# Group A — architecture (3 verbs)
#   architecture · isa_n6 · hexa1
# Group B — design (5 verbs)
#   design · dse_pipeline · rtl_gen · eda · verify_test
# Group C — process (5 verbs)
#   process · materials · wafer · yield · thermal_power
# Group D — packaging (6 verbs)
#   packaging · advanced_packaging · chip_3d · hbm · interconnect · sc
# Group E — accelerator (8 verbs)
#   npu_n6 · pim · photonic · accel · asic · hexa_pim · hexa_3d · hexa_wafer
# Group F — consciousness (2 verbs)
#   conscious_chip · conscious_soc
#
# NOTE — kick-spec one-liner reads "28-verb / 7-group"; the explicit
# verb_map enumeration totals 29 verbs across 6 group categories (the
# 7th "group" in the headline does not appear in the line-by-line map).
# This v1.0.0 honors the line-by-line map → 29 verbs / 6 groups.
[modules.architecture]
verbs = ["architecture", "isa_n6", "hexa1"]
[modules.design]
verbs = ["design", "dse_pipeline", "rtl_gen", "eda", "verify_test"]
[modules.process]
verbs = ["process", "materials", "wafer", "yield", "thermal_power"]
[modules.packaging]
verbs = ["packaging", "advanced_packaging", "chip_3d", "hbm", "interconnect", "sc"]
[modules.accelerator]
verbs = ["npu_n6", "pim", "photonic", "accel", "asic", "hexa_pim", "hexa_3d", "hexa_wafer"]
[modules.consciousness]
verbs = ["conscious_chip", "conscious_soc"]

# ── Meta-domain envelope (terafab) ─────────────────────────
# 메타-도메인은 verb 가 아니라 6 그룹 전체를 바깥에서 감싸는 외피.
# Terafab 은 "Tesla/xAI/SpaceX 의 수직-통합 megafab" 외부 관찰값을
# 흡수한 envelope 으로, 28-verb / 6-group 카운트는 변경하지 않는다.
# (절대로 새로운 verb 가 아님 — 6 그룹의 외부 wrapper 로만 등록.)
[meta_domains.terafab]
absorbs = ["architecture", "design", "process", "packaging", "accelerator", "consciousness"]
type = "vertically-integrated-megafab"
source = "external-research absorption (2026-05-11)"
entry = "terafab/terafab.md"
falsifier_count = 7      # F-TERAFAB-1..7
nda_content = false
note = "Outer envelope wrapping the 6 hexa-chip groups; NOT a verb. Does not change the 28-verb / 6-group counts."

# ── Meta-domain envelope (exynos) ──────────────────────────
# Sister envelope to terafab. Samsung Korean-fab heritage anchor.
# Exynos / Samsung Foundry / 40-year IDM heritage 의 외부 공시값만
# 흡수한 envelope (Samsung NDA / 내부 자료 / 영업기밀 일체 없음).
# 29-verb / 6-group 카운트는 변경하지 않는다 — 외피로만 등록.
[meta_domains.exynos]
absorbs = ["architecture", "design", "process", "packaging", "accelerator", "consciousness"]
type = "korean-fab-heritage"
source = "public-source absorption (2026-05-12)"
entry = "exynos/exynos.md"
falsifier_count = 7      # F-EXYNOS-1..7
nda_content = false
note = "Outer envelope wrapping the 6 hexa-chip groups under Samsung Exynos/Foundry heritage; NOT a verb. Public sources only (Samsung Foundry Forum / IR / IEEE / Korean tech press / TSMC-Intel earnings); zero NDA / zero Samsung-internal data."

# ── Dependencies ───────────────────────────────────────────
# All verbs are spec-first at v1.0.0 — no Python or external deps required.
[dependencies]
hexa-lang = ">=1.0.0"

# ── Closure provenance (raw#10 honest C3) ─────────────────
# v1.0.0 closure: 28-verb spec-first extraction. Working .hexa CLI dispatcher
# is a placeholder router that lists groups + verbs; per-verb sandboxes are
# spec-only at this release.
[closure]
verbs_total = 29
groups_total = 6
verbs_wired = 29       # 2026-05-07 buildout: every verb has verify_<verb>.hexa
verbs_spec = 29
verdict = "SPEC_PLUS_RUNNABLE"   # 2026-05-07 — was SPEC_FIRST at v1.0.0
extracted_from = "canon/domains/compute/ @ c0f1f570"
spec_count_caveat = "kick-spec one-liner read '28-verb / 7-group'; explicit verb_map enumerated 29 verbs across 6 group categories. v1.0.0 honors the line-by-line map."
runnable_surface = "verify/cli.hexa (5 checks) + verify/verb_runner.hexa (29 sandboxes) + tests/run_tests.hexa (4 tests) — make ci aggregates"

# ── Scope honesty (raw#10 C3) ─────────────────────────────
[scope]
honest_scope = "29-verb 통합 chip substrate (6 그룹: architecture + design + process + packaging + accelerator + consciousness). Spec-first; per-verb working .hexa CLI sandboxes TBD. Korea-fab heritage tone (Samsung·SK·Hynix)."
not_scope = [
    "Tape-out / mask-set generation (no GDSII writer)",
    "Foundry PDK distribution (no proprietary process kits vendored)",
    "EDA tool replacement (Cadence/Synopsys/Mentor stays canonical)",
    "Wet-fab calibration (out-of-repo; runs on physical wafers only)"
]
n6_lattice_caveat = "n=6 invariant referenced for ISA/architecture decomposition only (isa_n6, hexa1). Process/packaging/accelerator groups inherit organising convention without independent invariant-fit verification."

# ── Meta-domain closure (terafab + exynos envelopes, Wave 6~7) ──
# 메타-도메인 closure 는 verb-level closure 와 *별개의* 회계.
# 두 envelope (terafab Wave 6, exynos Wave 7) 은 28-verb / 6-group
# 표면을 변경하지 않고 외피로만 등록되며, 각자 falsifier register
# (terafab: F-TERAFAB-1..10 / exynos: F-EXYNOS-1..7) 와 runnable
# verify_*.py harness 로 closure 를 선언한다.
# (verb 카운트는 위 [closure] 가 단일 권위.)
[meta_domain_closure]
envelopes_total = 2                  # terafab + exynos
envelopes_wired = 2                  # both ship runnable verify_*.py
envelopes_audited = 2                # cross_doc_audit.py extended to audit both
falsifiers_total = 17                # F-TERAFAB-1..10 (10) + F-EXYNOS-1..7 (7)
falsifiers_runnable = 17             # all 17 dispatched by verify_terafab.py + verify_exynos.py
groups_wrapped = 6                   # all 6 hexa-chip groups absorbed by each envelope
verdict = "SPEC_PLUS_RUNNABLE"
verb_surface_unchanged = true        # verbs_total/groups_total NOT bumped
nda_content = false
extracted_from = "external-research absorption Wave 6 (Terafab 2026-05-11: Musk announce 2026-03-21, Intel join 2026-04-07, SpaceX TX filing 2026-05-06) + Wave 7 (Exynos 2026-05-12: Samsung Foundry Forum + IR + IEEE/IEDM + Korean tech press, all public)"
caveat = "Two meta-domain envelopes; F-TERAFAB-1..6 + F-EXYNOS-1..6 are bench-only at Mk.I (data-arrival pending); both F-7 χ² currently weak (p≈0.86 / p≈0.97), reformulation deferred to Mk.II per each envelope's roadmap. Exynos envelope is brownfield-IDM-heritage comparator to Terafab's greenfield-vertical-megafab claim — complementary, not redundant."

# ── chip-verify closure (Wave J — empirical harness promotion) ─────
# chip-verify/ promoted from T4 KNOWLEDGE (reference-only) to T3 RUNTIME
# (first-class verify surface). 22 imported .hexa scripts + 4 .md reports
# + 1 .json fixture, all dispatchable via chip-verify/cli.hexa and wired
# into `make ci` via verify/chip_verify_bridge.hexa. The 29-verb /
# 6-group surface is NOT changed by this block — chip-verify is an
# empirical sandbox, not a verb. The 34/36 boot-matrix headline (94.4%)
# from boot_matrix_report.md §1 is the documented aggregate; the 2/36
# (5.6%) failure cells stay visible (HEXA-TOPO × {Starlink, LoRaWAN}).
[chip_verify_closure]
scripts_total = 22                # imported .hexa files under chip-verify/
scripts_wired = 22                # all dispatchable via chip-verify/cli.hexa
reports_total = 4                 # boot_matrix_report.md + soc_bench_promotion_report.md + stage0_rerun_report.md + verify_chain.md
fixtures_total = 1                # boot_matrix_3x12.json
aggregate_pass_rate = 0.944       # 34/36 boot matrix headline (NOT 100%)
verdict = "SPEC_PLUS_RUNNABLE"
verb_surface_unchanged = true     # 29-verb / 6-group counts NOT bumped
nda_content = false
imported_from = "canon (Wave 5, hexa-chip commit 3f2c2b7)"
promoted_at = "Wave J (2026-05-12) — T4 KNOWLEDGE -> T3 RUNTIME"
runnable_surface = "chip-verify/cli.hexa (list/run/all/report/inventory) + verify/chip_verify_bridge.hexa (verify/cli.hexa dispatch) + Makefile target `make chip-verify`"
caveat = "Empirical chip verification harness; 34/36 boot matrix (94.4%) is the documented number; not 100%. Per-script harness aggregate yields PASS/PENDING/FAIL/ERROR mix (10 PASS / 8 PENDING / 1 FAIL / 3 ERROR observed at promotion); the 3 ERROR cases are stage0-double-main provenance (boot_matrix_3x12, verify_chip-3d, verify_protocol_bridge) preserved per the no-rewrite rule. The verify_test verb closure remains the authority for canonical verification — chip-verify is supplementary."

[cost]
mac_local_default = "$0 (spec-first; no external tooling invoked)"
